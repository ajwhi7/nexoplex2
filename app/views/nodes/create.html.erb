<% provide(:title, 'Create') %>
<h1>Create a new Node</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@node) do |f| %>

      <%= f.label :title %>
      <%= f.text_field :title %>

      <%= f.label :what %>
      <%= f.text_field :what %>

      <%= f.label :when %>
      <%= f.text_field :when %>
<h1>Create.html.erb, but it should show the created node</h1>
      <%= f.submit "Create Node", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
